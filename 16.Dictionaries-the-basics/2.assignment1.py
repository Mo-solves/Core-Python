# Create an empty dictionary and assignt it to the variable empty
empty = {}

# Create a dictionary with three key-value pairs
# The keys should be strings and the values should be integer values
# Assign the dictionary to a my_dic variable
my_dic = {
    'BirthYear': 1998,
    'favorite_number': 10,
    'weeks': 7
}

# A dictionary's keys can be any immutable data structures
# Create a dictionary with two key-value pairs and assing it to
# a winning_lottery_numbers variable
# Both of the keys should be tuples
# One of the values should be True, the other one should be False

winning_lettery_numbers = {
    (4, 9, 2, 8, 0, 6): True,
    (8, 5, 2, 6, 4, 1): True
}
