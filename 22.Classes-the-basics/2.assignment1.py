# Declare a country class with an empty body
class Country():
    pass


# Instantiate an object from the class and assignt it to an "America" var
america = Country()

# Instantiate another object from the class and assign it to a "Canada"var
canada = Country()

# Instantiate a third object from the class and assign it to a "mexico"var
mexico = Country()


# Declare a SuperHero class with an empty body
class SuperHero():
    pass


# Instantiate an object from the class and assignt it to an "spiderman"var
spiderman = SuperHero()

# Instantiate another object from the class and assigt it to a "batman"var
batman = SuperHero()

# Instantiate a third object from the class and assigt it to a "superman"
superman = SuperHero()
